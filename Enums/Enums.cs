namespace webserver.Enums
{
    public enum ErrorType
    {
        Success = 0,
        Failure = 1,
        BadRequest = 400,
        Unauthorized = 401,
        NotFound = 404,
        InternalServerError = 500
    }

}

